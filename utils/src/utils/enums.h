#ifndef ENUM_H
#define ENUM_H

typedef enum {
    SOYKERNEL,
    SOYCPU,
    SOYMEMORIA,
    SOYIO
} ID_MODULO;


typedef enum { //CODIGO DE OPERACION
    HANDSHAKE = 1,
    PETICION_IO, //INVENTADOS, NO SIRVEN PARA NADA
    RESPUESTA_PETICION_IO,
    CUATRO,
} CODIGO_OP;


#endif